{
  "scripts": {
    "dev": "set NODE_ENV=dev&&rollup -c --watch",
    "build": "set NODE_ENV=prod&&rollup -c"
  },
  "devDependencies": {
    "babel-preset-env": "^1.7.0",
    "rollup": "^1.0.1",
    "rollup-plugin-uglify": "^6.0.0"
  }
}
